# Frontend config example
# Base URL the frontend will call (use localhost for local dev)
VITE_API_BASE_URL=http://localhost:3000

# App mode / mock data
# If you don't provide MONGODB_URI on the backend, the server runs in MOCK data mode.
USE_MOCK=true
# MONGODB_URI=mongodb+srv://user:pass@host/dbname

# Optional / example variables - replace with real values in your local .env
NODE_ENV=development
PORT=5173
DEBUG=false

# Add other VITE_* environment variables here as needed
# e.g. VITE_MAPBOX_KEY=your_mapbox_key_here
